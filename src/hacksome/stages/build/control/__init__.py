"""Deterministic control plane for one HackSome Build Team.

The package intentionally imports no submodules. Current entrypoints are the
Team Hub, resident Lead loop, Worker Manager, and ephemeral Verifier runtime.
Import only the concrete module a process needs.
"""
